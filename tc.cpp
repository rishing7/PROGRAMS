#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    int arr[n];
    //memset(arr,0,sizeof(arr));
    memset(arr,5,sizeof(arr));
    for(int i=0;i<n;i++)
        cout<<arr[i]<<" ";
    return 0;
}
